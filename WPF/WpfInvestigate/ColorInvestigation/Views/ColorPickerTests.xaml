<Window x:Class="ColorInvestigation.Views.ColorPickerTests"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:controls="clr-namespace:ColorInvestigation.Controls"
        mc:Ignorable="d"
        Title="ColorPickerTests" Height="700" Width="800">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Grid Grid.Column="0" ZIndex="1" Background="White">
            <Slider Grid.Column="0"
                    x:Name="uiScaleSlider"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Top"
                    ToolTip="Determines the UI scale factor. Double-click to revert scaling back to 100%."
                    Height="100"
                    Value="1" Minimum="0.5" Maximum="4"
                    Orientation="Vertical"
                    Ticks="1"
                    IsSnapToTickEnabled="False"
                    TickPlacement="BottomRight"
                    AutoToolTipPlacement="BottomRight"
                    AutoToolTipPrecision="2"/>
        </Grid>

        <Grid Grid.Column="1">
            <Grid.LayoutTransform>
                <ScaleTransform CenterX="0" CenterY="0" ScaleX="{Binding ElementName=uiScaleSlider,Path=Value}" ScaleY="{Binding ElementName=uiScaleSlider,Path=Value}"/>
            </Grid.LayoutTransform>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <Rectangle Grid.Row="0" Width="50" Height="50" Margin="5" HorizontalAlignment="Left">
                <Rectangle.Fill>
                    <SolidColorBrush Color="{Binding ElementName=ColorPicker, Path=Color}"/>
                    <!--<SolidColorBrush Color="Blue"/>-->
                </Rectangle.Fill>
            </Rectangle>

            <Button Grid.Row="0" Content="Save Color" Margin="100 5 5 5" Height="24" Click="SaveButton_Click" HorizontalAlignment="Left"/>
            <Button Grid.Row="0" Content="Restore Color" Margin="200 5 5 5" Height="24" Click="RestoreButton_Click" HorizontalAlignment="Left"/>
            <Button Grid.Row="0" Content="Tests" Margin="300 5 5 5" Height="24" Click="TestButton_Click" HorizontalAlignment="Left"/>

            <!-- =============== -->
            <!--<code:ColorPicker BeforeBrush="#FFFF8040"></code:ColorPicker>-->
            <controls:ColorPicker Grid.Row="1" x:Name="ColorPicker"/>

        </Grid>

    </Grid>
</Window>
